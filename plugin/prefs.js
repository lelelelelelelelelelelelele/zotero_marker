// Optional Semantic Scholar API key — blank uses the free public endpoint (fine for normal
// use; only needed if you hit rate limits).
pref("extensions.zotero.zoteromarker.s2ApiKey", "");
// Items at or above this resolver confidence are pre-checked in the review dialog. Lower-
// confidence rows are shown but left unchecked (honest abstention — you opt them in).
pref("extensions.zotero.zoteromarker.minConfidence", "0.8");
